RUNNER_PORT=8200
RUNNER_TOKEN=your_runner_token

# PostgreSQL (Supabase) connection string
DATABASE_URL=postgres://user:password@localhost:5432/postgres

# Redis for job queue (Asynq)
REDIS_URL=redis://:password@localhost:6379/0

# NATS for report status events (optional)
NATS_URL=nats://localhost:4222

# Worker tuning
RUNNER_CONCURRENCY=4
RUNNER_QUEUE=analysis
ANALYZE_TIMEOUT=300s

# Pipeline engine
PIPELINE_QUEUE=pipelines
PIPELINE_CONCURRENCY=4
PIPELINE_RUN_TIMEOUT=2h
RUNNER_DATA_DIR=data
PIPELINE_LOG_RETENTION_DAYS=30
PIPELINE_ARTIFACT_RETENTION_DAYS=30

# Encryption key (same as studio)
ENCRYPTION_KEY=
